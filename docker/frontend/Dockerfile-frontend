# ビルドステージ（Node.js 24 slim）
FROM node:24-slim AS build-stage

WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# 本番ステージ（軽量なNginxでホスト）
FROM nginx:alpine AS production-stage

COPY --from=build-stage /app/dist /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
